<h1>Cats</h1>
<ul>
    <% @cats.each do |cat| %>
        <li><%= cat.name %><a href="<%= cat_url(cat.id) %>"><button>View</button></a>
        <a href="<%= edit_cat_url(cat.id) %>"><button>Edit</button></a>
        </li>
     <% end %>
</ul>